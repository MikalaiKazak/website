server:
  port: 9000

logging:
  level:
    root: INFO
    org.hibernate.SQL: DEBUG
    com.mikalaykazak.blog: DEBUG

spring:
  datasource:
    username: user
    password: password
    url: jdbc:postgresql://localhost:5432/blog
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true